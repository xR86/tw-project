package org.java_json_parser.OracleDBConnection;import java.sql.Connection;import java.sql.DriverManager;import java.sql.SQLException;import java.sql.Statement;/** * Created by lucai on 4/12/2016. */public class DBConnect {    private String user;    private String pass;    private Connection conn;    public DBConnect(String user, String pass){            this.user=user;            this.pass=pass;    }    public void connect() throws SQLException {        this.conn = DriverManager.getConnection("jdbc:oracle:thin:@//localhost:1522/XE", user, pass); //change data here    }    public void insert_query(int person_id, int task_id, String taskName, long date, String solution) throws SQLException {                Statement stmt = conn.createStatement();        try {            stmt.executeUpdate("INSERT INTO FreeCodeCamp VALUES("                    + person_id + ","                    + task_id + ","                    + "'" + taskName + "',"                    + "TO_TIMESTAMP(unix_ts_to_date(" + date + "),'YYYY-MM-DD HH24:MI:SS'),"                    + "'" + solution + "')");        } catch (SQLException e) {            e.printStackTrace();        }        //System.out.println(person_id + " " + task_id + " " + taskName + " " + date + " " + solution);        System.out.println("ROW INSERTED..");    }}